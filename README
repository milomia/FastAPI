1.to build docker image
  docker build -t fast_api .
2.to run
  docker compose up --build
3.to test
  goto to browser http://localhost:8000/docs
4.goto "startup to startup database (sqllite)
5.you can post a cake using the swagger ui
6. you can read the cake with the get
7. you can also delete the cake with the delete option
8. pytest tests included you will have to install local requirements and make your own virtual env

9. to run outside docker: uvicorn main::app --reload
10. to run tests outside docker: pytest tests

